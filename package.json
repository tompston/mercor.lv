{
  "name": "mercor",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && serve dist",
    "postbuild": "purgecss --css dist/assets/*.css --content dist/index.html -o dist/assets/  --safelist html body"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  },
  "dependencies": {
    "purgecss": "^4.1.3",
    "three": "^0.137.4"
  }
}
